#ifndef ROTATION_H
#define ROTATION_H

#include "point.h"
#include "correctInput.h"

int rotate(Point (&figureArray)[7], QString trStr,
                                        QString xStr, QString yStr);

#endif // ROTATION_H
